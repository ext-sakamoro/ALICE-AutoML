[package]
name = "alice-automl"
version = "1.0.0"
edition = "2021"
authors = ["Moroya Sakamoto"]
license = "MIT"
description = "AutoML: hyperparameter search, Bayesian optimization, NAS, early stopping, cross-validation"

[lib]
crate-type = ["rlib"]
